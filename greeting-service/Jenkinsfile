pipeline{
    agent any

    stages{
        stage('Deploy') {
            steps {
                sh '''
                    oc project tmyfcm-deploy-strategies
                    oc start-build greeting-service --follow --wait
                   '''
            }
        }
    }
}